$schema:            http://json-schema.org/draft-04/schema#
title:              "Put Artifact Response"
description: complete a blob artifact upload
type:               object
properties:
  outcome:
    type: string
    enum:
      - success
      - failure
additionalProperties: false
required:
  - outcome
